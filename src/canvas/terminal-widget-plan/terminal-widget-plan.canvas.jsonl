{"event":"canvas_created","timestamp":"2026-04-17T22:51:38.000Z","title":"Terminal Widget Plan","grid":true,"gridSize":24,"colorMode":"auto","widgets":[]}
{"event":"widget_added","timestamp":"2026-04-17T22:51:38.000Z","widget":{"id":"md-plan","type":"markdown","position":{"x":0,"y":0},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}}}
{"event":"widget_added","timestamp":"2026-04-17T22:51:38.000Z","widget":{"id":"story-scene1","type":"story","position":{"x":748,"y":0},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}}}
{"event":"widget_added","timestamp":"2026-04-17T22:51:38.000Z","widget":{"id":"story-scene2","type":"story","position":{"x":748,"y":444},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}}}
{"event":"widget_added","timestamp":"2026-04-17T22:51:38.000Z","widget":{"id":"story-scene3","type":"story","position":{"x":748,"y":928},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}}}
{"event":"widget_added","timestamp":"2026-04-17T22:51:38.000Z","widget":{"id":"story-scene4","type":"story","position":{"x":1552,"y":0},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}}}
{"event":"widget_added","timestamp":"2026-04-17T22:51:38.000Z","widget":{"id":"story-scene5","type":"story","position":{"x":1552,"y":544},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}}}
{"event":"widget_added","timestamp":"2026-04-17T22:51:38.000Z","widget":{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:34:27.005Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":466,"y":543},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":1214,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":1214,"y":987},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":1214,"y":1471},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":2018,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":2018,"y":1087},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:34:27.028Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":466,"y":543},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":1214,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":1214,"y":987},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":1214,"y":1471},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":2018,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":2018,"y":1087},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"widget_added","timestamp":"2026-04-18T06:35:14.446Z","widget":{"id":"image-zp1ic7","type":"image","position":{"x":884,"y":603},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}}}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:35:17.257Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":466,"y":543},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":1214,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":1214,"y":987},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":1214,"y":1471},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":2018,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":2018,"y":1087},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":460,"y":197},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:35:17.279Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":466,"y":543},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":1214,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":1214,"y":987},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":1214,"y":1471},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":2018,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":2018,"y":1087},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":460,"y":197},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}}]}
{"event":"widget_added","timestamp":"2026-04-18T06:35:33.044Z","widget":{"id":"image-rgvnrx","type":"image","position":{"x":702,"y":427},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}}}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:35:34.967Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":466,"y":543},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":1214,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":1214,"y":987},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":1214,"y":1471},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":2018,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":2018,"y":1087},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":460,"y":197},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":475,"y":286},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:35:34.990Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":466,"y":543},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":1214,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":1214,"y":987},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":1214,"y":1471},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":2018,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":2018,"y":1087},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":460,"y":197},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":475,"y":286},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:35:35.960Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":466,"y":543},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":1214,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":1214,"y":987},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":1214,"y":1471},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":2018,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":2018,"y":1087},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":468,"y":112},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":475,"y":286},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:35:35.988Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":466,"y":543},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":1214,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":1214,"y":987},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":1214,"y":1471},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":2018,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":2018,"y":1087},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":468,"y":112},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":475,"y":286},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:35:37.062Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":466,"y":543},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":1214,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":1214,"y":987},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":1214,"y":1471},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":2018,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":2018,"y":1087},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":468,"y":112},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":469,"y":217},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:35:37.084Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":466,"y":543},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":1214,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":1214,"y":987},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":1214,"y":1471},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":2018,"y":543},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":2018,"y":1087},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":468,"y":112},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":469,"y":217},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:35:58.864Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1457,"y":1457},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":468,"y":112},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":469,"y":217},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:35:58.888Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1457,"y":1457},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":468,"y":112},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":469,"y":217},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}}]}
{"event":"widget_added","timestamp":"2026-04-18T06:36:00.648Z","widget":{"id":"image-wxzfr4","type":"image","position":{"x":1523,"y":1237},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:36:02.482Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1457,"y":1457},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":468,"y":112},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":469,"y":217},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}},{"id":"image-wxzfr4","type":"image","position":{"x":1451,"y":1162},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:36:02.504Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1457,"y":1457},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":468,"y":112},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":469,"y":217},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}},{"id":"image-wxzfr4","type":"image","position":{"x":1451,"y":1162},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:36:06.374Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1457,"y":1457},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":1453,"y":460},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":1454,"y":565},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}},{"id":"image-wxzfr4","type":"image","position":{"x":1451,"y":1162},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:36:06.398Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1457,"y":1457},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":1453,"y":460},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":1454,"y":565},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}},{"id":"image-wxzfr4","type":"image","position":{"x":1451,"y":1162},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:36:09.043Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1457,"y":1457},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":1452,"y":740},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":1453,"y":845},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}},{"id":"image-wxzfr4","type":"image","position":{"x":1451,"y":1162},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:36:09.064Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1457,"y":1457},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":1452,"y":740},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":1453,"y":845},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}},{"id":"image-wxzfr4","type":"image","position":{"x":1451,"y":1162},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:36:11.384Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1457,"y":1457},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":1506,"y":761},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":1507,"y":866},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}},{"id":"image-wxzfr4","type":"image","position":{"x":1505,"y":1183},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:36:11.404Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1457,"y":1457},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":1506,"y":761},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":1507,"y":866},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}},{"id":"image-wxzfr4","type":"image","position":{"x":1505,"y":1183},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:36:12.192Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1457,"y":1457},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":1470,"y":733},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":1471,"y":838},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}},{"id":"image-wxzfr4","type":"image","position":{"x":1469,"y":1155},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:36:12.213Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1457,"y":1457},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":1470,"y":733},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":1471,"y":838},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}},{"id":"image-wxzfr4","type":"image","position":{"x":1469,"y":1155},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:37:01.016Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1174,"y":1463},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":1470,"y":733},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":1471,"y":838},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}},{"id":"image-wxzfr4","type":"image","position":{"x":1469,"y":1155},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:37:01.049Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1174,"y":1463},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":960,"height":680}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":1470,"y":733},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":1471,"y":838},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}},{"id":"image-wxzfr4","type":"image","position":{"x":1469,"y":1155},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:37:41.358Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1174,"y":1463},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"image-zp1ic7","type":"image","position":{"x":1470,"y":733},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-14.png","private":false,"width":600,"height":75}},{"id":"image-rgvnrx","type":"image","position":{"x":1471,"y":838},"props":{"src":"terminal-widget-plan--2026-04-18--08-35-33.png","private":false,"width":600,"height":280}},{"id":"image-wxzfr4","type":"image","position":{"x":1469,"y":1155},"props":{"src":"terminal-widget-plan--2026-04-18--08-36-00.png","private":false,"width":600,"height":208}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:38:28.406Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1174,"y":1463},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:38:28.598Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1174,"y":1463},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:38:31.072Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1430,"y":1454},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:38:31.094Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1430,"y":1454},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:19.511Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":884,"y":1409},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:19.537Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":884,"y":1409},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:21.812Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":884,"y":1409},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":1856,"y":1432},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:21.836Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":884,"y":1409},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":1856,"y":1432},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:22.499Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":884,"y":1409},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"source_updated","timestamp":"2026-04-18T06:46:22.499Z","sources":[]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:22.698Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":1430,"y":1454},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"source_updated","timestamp":"2026-04-18T06:46:22.698Z","sources":[]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:27.803Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:27.827Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2205,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}}]}
{"event":"widget_added","timestamp":"2026-04-18T06:46:40.104Z","widget":{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":2227,"y":1647},"props":{"text":"","color":"yellow","width":270,"height":170}}}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:43.261Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2022,"y":1356},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":2227,"y":1647},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:43.276Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2022,"y":1356},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":2227,"y":1647},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:44.247Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2022,"y":1356},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1686,"y":1704},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:44.267Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2022,"y":1356},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1686,"y":1704},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:45.688Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2199,"y":1429},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1686,"y":1704},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:45.718Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2199,"y":1429},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1686,"y":1704},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:48.045Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1686,"y":1704},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:48.066Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1686,"y":1704},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:49.578Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1883,"y":1638},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:49.603Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1883,"y":1638},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:51.460Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1892,"y":1638},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:46:51.480Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1892,"y":1638},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:49:32.598Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1962,"y":1563},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:49:32.622Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1962,"y":1563},"props":{"text":"","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T06:50:03.212Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1962,"y":1563},"props":{"text":"hello","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T07:04:28.372Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1945,"y":1574},"props":{"text":"hello","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T07:04:28.398Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1945,"y":1574},"props":{"text":"hello","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T07:04:33.531Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1946,"y":1608},"props":{"text":"hello","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T07:04:33.555Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1946,"y":1608},"props":{"text":"hello","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T07:04:59.385Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1946,"y":1608},"props":{"text":"Does a stop command makes sense here? Passing `ctrl + c` into the idle terminal?","color":"yellow","width":270,"height":170}}]}
{"event":"widget_added","timestamp":"2026-04-18T07:05:00.666Z","widget":{"id":"sticky-note-l0k7do","type":"sticky-note","position":{"x":1986,"y":1648},"props":{"text":"Does a stop command makes sense here? Passing `ctrl + c` into the idle terminal?","color":"yellow","width":270,"height":170}}}
{"event":"widgets_replaced","timestamp":"2026-04-18T07:05:02.788Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1946,"y":1608},"props":{"text":"Does a stop command makes sense here? Passing `ctrl + c` into the idle terminal?","color":"yellow","width":270,"height":170}},{"id":"sticky-note-l0k7do","type":"sticky-note","position":{"x":1949,"y":2104},"props":{"text":"Does a stop command makes sense here? Passing `ctrl + c` into the idle terminal?","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T07:05:02.812Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1946,"y":1608},"props":{"text":"Does a stop command makes sense here? Passing `ctrl + c` into the idle terminal?","color":"yellow","width":270,"height":170}},{"id":"sticky-note-l0k7do","type":"sticky-note","position":{"x":1949,"y":2104},"props":{"text":"Does a stop command makes sense here? Passing `ctrl + c` into the idle terminal?","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T07:05:09.883Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1946,"y":1608},"props":{"text":"Does a stop command makes sense here? Passing `ctrl + c` into the idle terminal?","color":"yellow","width":270,"height":170}},{"id":"sticky-note-l0k7do","type":"sticky-note","position":{"x":3903,"y":2281},"props":{"text":"Does a stop command makes sense here? Passing `ctrl + c` into the idle terminal?","color":"yellow","width":270,"height":170}}]}
{"event":"widgets_replaced","timestamp":"2026-04-18T07:05:09.900Z","widgets":[{"id":"md-plan","type":"markdown","position":{"x":772,"y":1430},"props":{"content":"# Terminal Widget + Copilot CLI Canvas Integration\n\n## Problem\n\nIntegrate a browser-based terminal into Storyboard canvases so users can trigger and steer Copilot CLI sessions directly from the canvas. This enables a workflow where canvas context (selected widgets, sticky notes, images) can feed into Copilot CLI prompts, and results can flow back as new canvas widgets.\n\n## Feasibility Assessment\n\n**ghostty-web** is a strong fit:\n- xterm.js-compatible API, drop-in React integration\n- ~400KB WASM bundle (acceptable for dev-only widget)\n- Zero runtime dependencies\n- Active maintenance (Coder uses it in Mux)\n\n**Key constraint**: ghostty-web is a *frontend terminal emulator only*. It needs a WebSocket backend that spawns a PTY process. This means:\n- **Dev-only is straightforward** — add a WebSocket PTY server to the Vite dev server plugin (`packages/core/src/vite/server-plugin.js`)\n- **Prod would require a hosted backend** — out of scope for v1\n\n## Approach\n\n**Phase 1**: General-purpose terminal widget + Copilot CLI launcher (dev-only, shipped together)\n**Phase 2**: Full canvas↔Copilot bidirectional integration (future)\n\n## Architecture\n\n```\n┌─────────────────────────────────┐\n│  Canvas (browser)               │\n│  ┌───────────────────────────┐  │\n│  │  TerminalWidget.jsx       │  │\n│  │  ├─ ghostty-web Terminal  │  │\n│  │  └─ WebSocket client      │  │\n│  └───────────────────────────┘  │\n└──────────────┬──────────────────┘\n               │ ws://localhost:PORT/_storyboard/terminal/:sessionId\n┌──────────────▼──────────────────┐\n│  Vite Server Plugin             │\n│  ├─ WebSocket upgrade handler   │\n│  ├─ node-pty PTY spawn          │\n│  └─ Session manager (per tab)   │\n└─────────────────────────────────┘\n```\n\n## Todos\n\n### Phase 1 — Terminal Widget (dev-only)\n\n1. **pty-backend** — Add WebSocket PTY server to `packages/core/src/canvas/terminal-server.js`\n   - Use `node-pty` to spawn shells\n   - WebSocket upgrade on `/_storyboard/terminal/:sessionId`\n   - Wire into `server-plugin.js` via Vite's `configureServer` hook\n   - Session lifecycle: create on connect, destroy on disconnect\n   - Resize support (SIGWINCH via cols/rows messages)\n\n2. **widget-config** — Add `terminal` widget type to `widgets.config.json`\n   - Props: `width`, `height`, `cwd` (optional working directory)\n   - Features: clear, restart, copy-link, delete\n   - Dev-only (no `prod: true` on features)\n\n3. **widget-component** — Create `TerminalWidget.jsx` in `packages/react/src/canvas/widgets/`\n   - Import ghostty-web (`init()` + `Terminal`)\n   - Connect to WebSocket backend using widget ID as session ID\n   - Handle resize via ResizeHandle (send cols/rows to backend)\n   - Theme sync (use canvas theme for terminal colors)\n   - Register in `widgetRegistry` (index.js)\n\n4. **widget-styles** — Create `TerminalWidget.module.css`\n   - Terminal container fills widget bounds\n   - Focus/blur handling (canvas keyboard shortcuts vs terminal input)\n   - Scrollbar styling to match canvas theme\n\n5. **paste-rule** — Optional: add paste rule so pasting a shell command creates a terminal widget\n   - Low priority, can skip for v1\n\n### Phase 2 — Copilot CLI Integration\n\n6. **copilot-launcher** — Add a \"Run Copilot\" toolbar action on the terminal widget\n   - Sends `copilot` command to the terminal's PTY\n   - Could pre-populate with context from selected widgets on the same canvas\n\n7. **canvas-context-injection** — When launching Copilot from a terminal widget:\n   - Read `.storyboard/.selectedwidgets.json` for current canvas state\n   - Serialize relevant widget content (sticky note text, markdown, image descriptions)\n   - Pass as initial prompt or piped stdin to `copilot`\n\n8. **output-capture** — Monitor terminal output for structured markers\n   - Copilot CLI could emit structured output (e.g., code blocks, file diffs)\n   - Parse and offer \"Add to canvas\" actions for recognized output\n\n### Phase 3 — Full Bidirectional Integration (future)\n\n9. **canvas-copilot-bridge** — Purpose-built Copilot widget (not just a terminal)\n   - Prompt input field with canvas context auto-attached\n   - Streaming response display\n   - One-click \"create sticky from response\" / \"create markdown from response\"\n\n10. **multi-agent-canvas** — Multiple terminal widgets on one canvas\n    - Each running its own Copilot session\n    - Shared canvas context, independent sessions\n\n## Dependencies & Risks\n\n| Risk | Mitigation |\n|------|-----------|\n| `node-pty` requires native compilation | It's widely used (VS Code uses it). Add to `optionalDependencies` so install doesn't fail on unsupported platforms |\n| ghostty-web WASM bundle size (~400KB) | Lazy-load only when a terminal widget is on the canvas. Dev-only so not in prod bundle |\n| Keyboard focus conflicts (canvas shortcuts vs terminal input) | Terminal widget captures focus on click, releases on Escape or click-outside. Similar to how markdown edit mode works |\n| Security: PTY gives full shell access | Dev-only, same trust model as the Vite dev server itself |\n| ghostty-web maturity | xterm.js is a proven fallback if ghostty-web has issues. API is compatible so switching is trivial |\n\n## Package Changes\n\n- **New dependency**: `ghostty-web` (frontend, lazy-loaded)\n- **New dependency**: `node-pty` (backend, optional, dev-only)\n- **Modified**: `packages/core/src/vite/server-plugin.js` (WebSocket upgrade)\n- **Modified**: `packages/core/widgets.config.json` (new widget type)\n- **Modified**: `packages/react/src/canvas/widgets/index.js` (registry)\n- **New files**: `terminal-server.js`, `TerminalWidget.jsx`, `TerminalWidget.module.css`\n\n## Open Questions\n\n1. **ghostty-web vs xterm.js**: ghostty-web is newer and shinier but xterm.js is battle-tested. Worth prototyping with ghostty-web first since the API is compatible — easy to swap.\n2. **Session persistence**: Should terminal sessions survive page reload (reconnect to existing PTY)? Adds complexity but is very useful.\n3. **Multi-tab**: Each browser tab gets its own PTY? Or shared sessions via session ID?\n4. **CWD default**: Should the terminal widget default to the repo root, or the canvas directory?","width":700,"height":1200}},{"id":"story-scene1","type":"story","position":{"x":2203,"y":1444},"props":{"storyId":"terminal-widget-mock","exportName":"EmptyTerminal","width":780,"height":420}},{"id":"story-scene2","type":"story","position":{"x":2205,"y":1901},"props":{"storyId":"terminal-widget-mock","exportName":"RunningCommand","width":780,"height":460}},{"id":"story-scene3","type":"story","position":{"x":2205,"y":2385},"props":{"storyId":"terminal-widget-mock","exportName":"CopilotLauncher","width":780,"height":480}},{"id":"story-scene4","type":"story","position":{"x":3009,"y":1457},"props":{"storyId":"terminal-widget-mock","exportName":"CanvasContext","width":960,"height":520}},{"id":"story-scene5","type":"story","position":{"x":3009,"y":2001},"props":{"storyId":"terminal-widget-mock","exportName":"FullFlow","width":934,"height":791}},{"id":"sticky-title","type":"sticky-note","position":{"x":0,"y":-120},"props":{"text":"🖥️ Terminal Widget + Copilot CLI Canvas Integration — Visual Storyboard","color":"purple","width":700,"height":90}},{"id":"sticky-note-3wpofl","type":"sticky-note","position":{"x":1946,"y":1608},"props":{"text":"Does a stop command makes sense here? Passing `ctrl + c` into the idle terminal?","color":"yellow","width":270,"height":170}},{"id":"sticky-note-l0k7do","type":"sticky-note","position":{"x":3903,"y":2281},"props":{"text":"Does a stop command makes sense here? Passing `ctrl + c` into the idle terminal?","color":"yellow","width":270,"height":170}}]}
